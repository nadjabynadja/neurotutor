# NeuroTutor Backend Dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
opencv-python>=4.8.0
numpy>=1.24.0
python-multipart>=0.0.6

# Optional: For EEG integration
brainflow>=4.8.0

# For LLM integration (when ready)
openai>=1.0.0
anthropic>=0.18.0

# For database (when ready)
sqlalchemy>=2.0.0
asyncpg>=0.29.0
redis>=5.0.0

# For auth (when ready)
python-jose>=3.3.0
passlib>=1.7.4
